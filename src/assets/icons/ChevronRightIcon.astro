---
import chevronRightSvg from "./chevron-right.svg?raw";

interface Props {
  class?: string;
}

const { class: className = "w-5 h-5" } = Astro.props;
const svgWithClass = chevronRightSvg.replace(/<svg([^>]*)>/, `<svg$1 class="${className}">`);
---

<Fragment set:html={svgWithClass} />
